// Standard, I use this for larger functions:
function myFunction(x: number, y: number): number {
  return x + x * y
}

// Arrow function, I use this for smaller functions
const myFunctionArrow = (x: number, y: number) => x + x * y
